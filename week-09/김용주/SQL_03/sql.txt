WITH RECURSIVE ZTIME(HOUR) AS (
    SELECT 0
    UNION
    SELECT ZTIME.HOUR + 1 FROM ZTIME WHERE ZTIME.HOUR < 23
)

SELECT HOUR, COUNT(ANIMAL_OUTS.DATETIME) as COUNT
FROM ZTIME LEFT JOIN ANIMAL_OUTS  ON HOUR = HOUR(DATETIME)
GROUP BY HOUR
ORDER BY HOUR;